export const environment = {
  production: false,
  // Leave empty — the user sets the live Cloudflare URL in the Configuration page.
  // It is persisted in localStorage under the key 'arcane_api_url'.
  apiUrl: ''
};
